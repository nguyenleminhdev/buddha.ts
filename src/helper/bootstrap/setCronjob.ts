// import { is_master_node, new_cronjob } from 'buddha-core'

// import type { CbError } from 'buddha-core'

// if (is_master_node()) new_cronjob(
//     {
//         date: '1 * * * * *', // chạy khi số giây = 1 ~ 1 phút một lần
//     },
//     () => {
        
//     }
// )